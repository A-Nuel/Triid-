import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function test() { const {data:r} = await supabase.from('users').select('id').eq('role','resident').limit(1); const {data:a} = await supabase.from('users').select('id').eq('role','artisan').limit(1); const {data:job, error:e1} = await supabase.from('jobs').insert({resident_id: r[0].id, artisan_id: a[0].id, mode: 'scheduled', category: 'other', description: 'test', location: 'POINT(3.3792 6.5244)', status: 'pending'}).select('id').single(); console.log(e1 || job); const {error:e2} = await supabase.from('messages').insert({job_id: job.id, sender_id: r[0].id, receiver_id: a[0].id, content: 'test msg'}); console.log(e2 || 'msg success'); } test();
